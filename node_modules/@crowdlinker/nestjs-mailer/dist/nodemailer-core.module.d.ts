import { NodemailerOptions, NodemailerAsyncOptions } from './interfaces';
import { NodemailerDrivers } from './constants';
import { DynamicModule } from '@nestjs/common';
export declare class NodemailerCoreModule {
    static forRoot<T extends NodemailerDrivers>(options: NodemailerOptions<T>): DynamicModule;
    static forRootAsync<T extends NodemailerDrivers>(options: NodemailerAsyncOptions<T>): DynamicModule;
    private static createAsyncProviders;
    private static createAsyncOptionsProvider;
}
